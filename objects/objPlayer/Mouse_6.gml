/*other.rbmsg = ""

with (parJoint)
	if (root == other.id and name == "Shoulders")
		other.rbmsg += name+"\n Degree: "+string(target)+"\n\n"
with (parJoint)
	if (root == other.id and name == "Arm Left")
		other.rbmsg += name+"\n Degree: "+string(target)+"\n\n"
with (parJoint)
	if (root == other.id and name == "Front Arm Left")
		other.rbmsg += name+"\n Degree: "+string(target)+"\n\n"
with (parJoint)
	if (root == other.id and name == "Hand Left")
		other.rbmsg += name+"\n Degree: "+string(target)+"\n\n"
with (parJoint)
	if (root == other.id and name == "Arm Right")
		other.rbmsg += name+"\n Degree: "+string(target)+"\n\n"
with (parJoint)
	if (root == other.id and name == "Front Arm Right")
		other.rbmsg += name+"\n Degree: "+string(target)+"\n\n"
with (parJoint)
	if (root == other.id and name == "Hand Right")
		other.rbmsg += name+"\n Degree: "+string(target)+"\n\n"

show_message(rbmsg)*/