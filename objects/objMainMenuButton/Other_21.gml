if (text == "New Game" or text == "Yeni Oyun")
	text = eng() ? "New Game" : "Yeni Oyun"

if (text == "Continue" or text == "Devam Et")
	text = eng() ? "Continue" : "Devam Et"

if (text == "Load" or text == "Yükle")
	text = eng() ? "Load" : "Yükle"

if (text == "Options" or text == "Ayarlar")
	text = eng() ? "Options" : "Ayarlar"

if (text == "Exit Game" or text == "Oyunu Kapat")
	text = eng() ? "Exit Game" : "Oyunu Kapat"
	
if (text == "Default" or text == "Varsayılan")
	text = eng() ? "Default" : "Varsayılan"
	
if (text == "Main Menu" or text == "Ana Menü")
	text = eng() ? "Main Menu" : "Ana Menü"