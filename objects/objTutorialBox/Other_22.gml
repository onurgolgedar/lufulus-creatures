if (isQuestion(1)) {
	/*if (isButton(0))
		show_messagebox(400, 400, text, title)
	else */if (isButton(1)) {
		ds_list_add(global.takenTutorials, tutorialNo)
		save_general(global.saveNo)
		
		alarm[1] = 3
	}
}